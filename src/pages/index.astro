---
import Layout from '../layouts/Layout.astro';
import WorksheetFilter from '../components/WorksheetFilter.astro';
import { ghostClient } from '../lib/ghost';
import { downloadGhostAsset } from '../utils/downloadGhostAsset';

const lang = 'english'; // Default language

let posts: any[] = [];

try {
  posts = await ghostClient.posts.browse({
      limit: "12",
      filter: `tag:${lang}`,
      include: ['tags']
  });
  posts = await Promise.all(posts.map(async (post: any) => {
    let feature_image = post.feature_image;
    if (feature_image) {
      feature_image = await downloadGhostAsset(feature_image, post.slug);
    }
    return { ...post, feature_image };
  }));
} catch (e) {
  console.error("Failed to fetch Ghost posts", e);
}
---
<Layout 
  title="Teacher Jake Worksheets - English" 
  currentLang={lang} 
  currentTag="all"
  description="Browse our collection of interactive English language learning materials, reading exercises, and quizzes."
>
  <div class="mb-8 text-center pt-4">
    <h1 class="text-4xl md:text-5xl font-extrabold mb-4 text-[#06422f] tracking-tight">English Worksheets</h1>
    <p class="text-lg text-[#4a5568] max-w-2xl mx-auto">Browse our collection of interactive English language learning materials, reading exercises, and quizzes.</p>
  </div>
  
  {posts.length === 0 ? (
    <div class="bg-amber-50 text-amber-800 p-6 rounded-xl border border-amber-200 text-center shadow-sm">
      <p class="text-lg font-medium">No English worksheets found.</p>
      <p class="mt-2 opacity-80">Make sure your Ghost posts have the tag "English". Check your Ghost API Key in .env to ensure connection.</p>
    </div>
  ) : (
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4" data-pagefind-body transition:name="content-grid">
      {posts.map((post: any) => {
        const levelTag = post.tags?.find((tag: any) => ['A1', 'A2', 'B1', 'B2'].includes(tag.name));
        return (
        <a href={`/gh/${post.slug}`} class="group bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden hover:shadow-xl hover:-translate-y-1 transition-all duration-300 flex flex-col h-full">
          <div class="aspect-[16/9] w-full flex-shrink-0 bg-slate-50 loading-pulse flex items-center justify-center overflow-hidden relative border-b border-slate-100">
            {post.feature_image ? (
               <img 
                 src={post.feature_image} 
                 alt={post.title} 
                 class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" 
                 loading="lazy"
                 decoding="async"
               />
            ) : (
               <div class="w-full h-full flex items-center justify-center text-emerald-200">
                 <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
               </div>
            )}
          </div>
          
          <div class="flex-grow p-5 flex flex-col">
            <div class="flex items-center gap-2 mb-3">
              {levelTag && (
                <span class="text-[10px] font-black tracking-wider text-emerald-700 uppercase bg-emerald-50 px-2 py-0.5 rounded border border-emerald-100">
                  {lang.substring(0, 2).toUpperCase()}-{levelTag.name}
                </span>
              )}
              <div class="flex items-center gap-2">
                {levelTag && <div class="h-1 w-1 rounded-full bg-slate-200"></div>}
                {(() => {
                  const displayTag = post.tags?.find((t: any) => 
                    !['A1', 'A2', 'B1', 'B2', lang].includes(t.name) && 
                    !t.name.startsWith('#')
                  );
                  return displayTag ? (
                    <span class="text-[10px] font-black tracking-wider text-emerald-700 uppercase bg-emerald-50 px-2 py-0.5 rounded border border-emerald-100">#{displayTag.name}</span>
                  ) : (
                    <span class="text-[10px] text-slate-400 font-medium tracking-wider uppercase">#{post.slug.substring(0, 4)}</span>
                  );
                })()}
              </div>
            </div>

            <h2 class="font-extrabold text-blue-600 text-[1.15rem] leading-tight mb-2 group-hover:text-blue-800 transition-colors line-clamp-2 min-h-[3rem]">
              {post.title}
            </h2>
            
            <p class="text-slate-500 text-sm leading-relaxed line-clamp-2 mb-0 flex-grow">
              {post.custom_excerpt || post.excerpt}
            </p>
            
            <div class="pt-4 mt-auto border-t border-slate-100 flex items-center justify-between">
                <span class="text-[11px] font-extrabold tracking-widest text-blue-600 uppercase">Start Worksheet</span>
                <div class="w-8 h-8 rounded-full bg-blue-50 flex items-center justify-center text-blue-600 group-hover:bg-blue-600 group-hover:text-white transition-all duration-300 shadow-sm group-hover:shadow-md">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                </div>
            </div>
          </div>
        </a>
      )})}
    </div>

  )}
  
  <div class="flex justify-center mt-12 mb-8">
      <a href={`/${lang}/2`} class="px-6 py-2.5 bg-white border border-slate-200 rounded-lg text-[#06422f] hover:bg-[#f0fdf4] hover:border-[#86efac] font-semibold transition-colors flex items-center gap-2 shadow-sm">
        Next Page <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
      </a>
  </div>
</Layout>
